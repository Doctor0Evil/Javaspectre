# Pattern: ALN Schema

A neutral pattern for defining ALN schemas.

Principles:

- Use clear field definitions
- Add semantic constraints
- Keep schemas domain-agnostic
- Avoid embedding runtime-specific logic
